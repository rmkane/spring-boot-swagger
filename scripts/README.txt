📂 How to Decode This Archive

After extracting the archive:

1. Rename `scripts/convert.sh.txt` to `scripts/convert.sh`
2. Make it executable (if needed): `chmod +x scripts/convert.sh`
3. Run the following command to restore all filenames:

   ./scripts/convert.sh decode
